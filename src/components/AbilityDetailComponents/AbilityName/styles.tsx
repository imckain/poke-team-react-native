import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  name: {
    color: "#fff",
    fontSize: 38,
    fontWeight: "600",
    marginVertical: 8,
    marginBottom: 12,
    textTransform: "capitalize",
  },
});
